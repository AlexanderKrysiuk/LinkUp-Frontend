import React from 'react';

const RefGroupTitle = (title: string) => {
	return (
		<div className='footer__reference-category-title'>
			<h4>{title}</h4>
		</div>
	);
};

export default RefGroupTitle;
